export enum Links {
  Main = '/',
  Game = '/game',
  Results = '/results',
  NotFound = '*'
}

export enum UserInfo {
  Name = "name",
  TypeGame = "type",
  StepsCount =  "stepsCount",
  Difficulty = "difficulty"
}

export enum GameDifficulty {
  Easy = "easy",
  Middle = "middle",
  Hard = "hard"
}

export enum SettingGame {
  CounterTime= 60,
  LivesCount = 6
}
